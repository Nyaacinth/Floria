{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile for Publish",
            "type": "shell",
            "command": "bun run compile",
            "group": { "kind": "build", "isDefault": true }
        }
    ]
}
