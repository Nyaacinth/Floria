{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch App in Development Mode",
            "type": "node-terminal",
            "request": "launch",
            "command": "bun run tauri dev"
        }
    ]
}
