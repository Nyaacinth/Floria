{
    "name": "work.nyaacinth.floria",
    "description": "Floria App",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "main": "packages/entry-point.js",
    "packageManager": "bun@1.3.9",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "bun run --workspaces --if-present build",
        "compile": "bun run build && electron-builder build --config electron-builder.js",
        "test": "bun run playwright test ./tests/e2e.spec.ts",
        "start": "bun run packages/dev-mode.js",
        "typecheck": "bun run --workspaces --if-present typecheck"
    },
    "devDependencies": {
        "@npmcli/map-workspaces": "5.0.3",
        "@playwright/test": "1.58.2",
        "@types/node": "25.2.3",
        "electron": "40.4.1",
        "electron-builder": "26.7.0",
        "glob": "13.0.5",
        "playwright": "1.58.2",
        "prettier": "3.8.1"
    },
    "dependencies": {
        "@app/main": "workspace:*"
    }
}
